import * as $wml from '@quenk/wml';
import * as B from '../Button';
export declare class Group extends $wml.AppView<B.Group> {
    constructor(context: B.Group);
}
export declare class Button extends $wml.AppView<B.Button> {
    constructor(context: B.Button);
}
