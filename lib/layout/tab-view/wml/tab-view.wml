import { Tabs,Tab } from "../../../control/tabs"
import {TabView} from ".."

{% main (TabView) %}

<div ww:class={{@values.root.class}}>

  <Tabs>

    {% for tab,name:String in @values.tabs %}
  
      <Tab 
        ww:name={{name}}
        ww:active={{@values.tab == name}}
        ww:onClick={{@values.onClick}}>

          {% if tab.tabContent %}

           {{<tab.tabContent(@)>}}

          {% else if tab.text %}

            {{tab.text}}

          {% else %}

            {{name}}

          {% endif %}

      </Tab>

    {% endfor %}

  </Tabs>

  {% if @values.content %}

    {{@values.content}}

  {% else %}

    {{@children}}

  {% endif %}

</div>
