{% import (MenuIcon) from "../" %}

{% view Main (MenuIcon) %}

<span id={{@values.root.id}} class={{@values.root.className}}>

  <span class={{@values.dash.class}} />
  <span class={{@values.dash.class}} />
  <span class={{@values.dash.class}} />

</span>

