{% import (Demo) from "../../../widgets/demo" %}
{% import (DateField) from "../../../../../../lib/control/date-field" %}
{% import (DateFieldPage) from "../" %}

{% view Main (DateFieldPage) %}
<Demo>

  <p>The date is : <b wml:id="selected">(None selected)</b>.</p>

  <p>
    <DateField
      wml:id="date"
      ww:name="date"
      ww:date="Date Label"
      ww:message="Enter the date in the format DD MM YYYY"
      ww:onChange={{@onChange}} />
  </p>

 </Demo>
