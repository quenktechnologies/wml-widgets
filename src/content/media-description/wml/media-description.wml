{% import * as m from "../" %}

{% view MediaDescription (m.MediaDescription) %}
<div id={{@values.id}} class={{@values.className}}>{{...@children}}</div>

{% view Media (m.Media) %}
<div id={{@values.id}} class={{@values.className}}>{{...@children}}</div>

{% view Description (m.Description) %}
<div id={{@values.id}} class={{@values.className}}>{{...@children}}</div>
