
{% import (Label) from "../label" %}

{% view TextFacadeView (TextFacade from ".") %}
<div class={{@wrapper.className}}>
    {% if @label %}
      <Label for={{@id}} text={{@label}} />
    {% endif %}
    <div id={{@id}} 
         class={{@className}} 
         onclick={{@onclick}}>
        {{...@children}}
    </div>
</div>
