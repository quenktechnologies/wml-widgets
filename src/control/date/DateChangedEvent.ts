import { FormControlEvent } from '@package/self/control/form-control';

/**
 * DateChangedEvent is generated when the date has 
 * been changed to a valid date.
 */
export class DateChangedEvent extends FormControlEvent<string> { }
