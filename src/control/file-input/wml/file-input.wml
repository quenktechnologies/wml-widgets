{% import (FileInput) from "../" %}

{% view FileInputView (FileInput) %}
<input 
  id={{@values.id}}
  class={{@values.className}} 
  name={{@values.name}}
  type="file"
  accept={{@values.accept}} 
  onchange={{@values.change}} 
  multiple={{@values.multiple}} />

