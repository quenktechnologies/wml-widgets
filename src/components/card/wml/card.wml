import {combine} from 'wml-widgets-common/util'

{% view Card %}

  <div 
    class={{combine(["card", @.read('ww:class')])}}>{{this.children}}</div>

{% endview %}

{% view CardBody %}

  <div 
    class={{combine(["card-body",@.read('ww:class')])}}>{{this.children}}</div>

{% endview %}

